https://d6qdbhpn-5000.euw.devtunnels.ms

# ВНИМАНИЕ: файл совместимости со СТАРЫМИ клиентами (до версии 2.0.0).
#
# Клиенты, выпущенные до переезда на раскладку Tebegram-client/, берут адрес
# сервера ТОЛЬКО отсюда и другого пути не знают:
#   https://raw.githubusercontent.com/AWPMasterGames/Tebegram/refs/heads/main/Tebegrammmm/Adress.txt
# Если файла нет, они получают 404 и молча уходят на localhost:5000 —
# со стороны это выглядит как «клиент не подтягивает адрес с main».
#
# Поэтому адрес в ПЕРВОЙ строке нужно держать таким же, как в
# Tebegram-client/Adress.txt. Клиенты 2.0.0 и новее читают только его,
# а этот файл игнорируют (оба разбирают лишь первую строку, поэтому
# комментарии ниже безопасны).
#
# Файл можно будет удалить, когда у всех тестеров будет клиент 2.0.0+.
