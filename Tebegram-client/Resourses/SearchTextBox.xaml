<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <ControlTemplate x:Key="TextBoxBaseControlTemplate" TargetType="{x:Type TextBoxBase}">
        <Border
            x:Name="Bd"
            Padding="8,6"
            Background="{TemplateBinding Background}"
            BorderBrush="{DynamicResource Light.BorderDefaultBrush}"
            BorderThickness="{TemplateBinding BorderThickness}"
            CornerRadius="10">
            <ScrollViewer x:Name="PART_ContentHost" VerticalAlignment="Center" />
        </Border>
        <ControlTemplate.Triggers>
            <!--  Кисти темы, а не SystemColors: во время входа поля отключаются,
                  и с системной серой кистью «слетала» тема окошка логина  -->
            <Trigger Property="IsEnabled" Value="False">
                <Setter TargetName="Bd" Property="Background" Value="{DynamicResource Light.BgElevatedBrush}" />
                <Setter Property="Foreground" Value="{DynamicResource Light.TextDisabledBrush}" />
            </Trigger>
            <Trigger Property="Width" Value="Auto">
                <Setter Property="MinWidth" Value="100" />
            </Trigger>
            <Trigger Property="Height" Value="Auto">
                <Setter Property="MinHeight" Value="20" />
            </Trigger>
        </ControlTemplate.Triggers>
    </ControlTemplate>
</ResourceDictionary>