<Window
    x:Class="Tebegrammmm.EditContactNameWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    Title="Изменить имя контакта"
    Width="400"
    Height="200"
    ResizeMode="NoResize"
    WindowStartupLocation="CenterScreen">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <TextBlock
            Grid.Row="0"
            Margin="0,0,0,10"
            FontSize="14"
            Text="Введите новое имя контакта:" />

        <TextBox
            x:Name="TBName"
            Grid.Row="1"
            Height="30"
            Padding="5"
            FontSize="14" />

        <StackPanel
            Grid.Row="3"
            Margin="0,20,0,0"
            HorizontalAlignment="Right"
            Orientation="Horizontal">
            <Button
                x:Name="BtnSave"
                Width="80"
                Height="30"
                Margin="0,0,10,0"
                Click="Button_Save_Click"
                Content="Сохранить"
                IsDefault="True" />
            <Button
                x:Name="BtnCancel"
                Width="80"
                Height="30"
                Click="Button_Cancel_Click"
                Content="Отмена"
                IsCancel="True" />
        </StackPanel>
    </Grid>
</Window>