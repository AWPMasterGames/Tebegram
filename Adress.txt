https://wd11t63t-5000.euw.devtunnels.ms

# Адрес сервера. Первая строка — сам адрес, всё ниже клиенты не читают.
#
# Это КАНОНИЧЕСКИЙ файл: именно его правят при смене адреса туннеля.
# Клиенты берут первую строку по ссылке:
#   https://raw.githubusercontent.com/AWPMasterGames/Tebegram/refs/heads/main/Adress.txt
#
# ВАЖНО: пока в ходу установленные клиенты 2.0.0, тот же адрес нужно
# продублировать в Tebegram-client/Adress.txt — у них корневого пути в цепочке
# ещё нет. Когда все обновятся, зеркало можно удалить.
