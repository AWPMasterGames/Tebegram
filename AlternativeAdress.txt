https://d6qdbhpn-5000.euw.devtunnels.ms

# Запасной адрес сервера. Первая строка - сам адрес, всё ниже клиенты не читают.
#
# Второй сервер команды. Клиенты проверяют источники по очереди и берут первый,
# который отвечает на /Test строкой HI!. Порядок такой:
#   1) Adress.txt в корне       - основной сервер
#   2) AlternativeAdress.txt    - этот файл
#   3) Tebegram-client/Adress.txt и запасная ветка
#
# Поэтому пока основной сервер запущен, до этого файла очередь не доходит.
# Когда основной выключен, клиенты сами переходят сюда, ничего настраивать
# вручную не нужно.
#
# Адрес меняется при каждом перезапуске туннеля: после запуска devtunnel
# скопировать новую ссылку сюда и закоммитить в ветку main.
